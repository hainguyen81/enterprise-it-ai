# 📥 SYSTEM DATA INPUT CHANNEL
- **Target Project Identity Name**: "{{ project_name }}"
- **Document Control Tracking ID**: "MKT-{{ doc_id }}"
- **System Generation Timestamp**: "{{ current_timestamp }}"

# CORPORATE COMPLIANCE RULES & GUIDELINES
```markdown
{{ raw_compliance_content }}
```

# RAW MARKETING PLANNER SOURCE REFERENCE (THE PLAN TO AUDIT)
```markdown
{{ raw_planner_content }}
```

# APPROVED MARKETING DATA ASSET REFERENCE (THE CORE INFRASTRUCTURE TRUTH)
[CONTEXT STORAGE ONLY - DO NOT ADOPT THIS LAYOUT]
```markdown
{{ raw_asset_content }}
```

# HIGH-PRIORITY AUDITING DIRECTIVES
1. Carefully audit the campaign focuses, editorial topics, and intervals inside the **RAW MARKETING PLANNER SOURCE REFERENCE** against the factual technology stack defined in the **APPROVED MARKETING DATA ASSET REFERENCE**.
2. Cross-examine the entire strategy layout to verify 100% alignment with the compliance protocols in the **CORPORATE COMPLIANCE RULES & GUIDELINES**. Ensure no metric inflation or banned terms are planned.

# EXECUTIVE TERMINATION CONSTRAINT (ANTI-DUPLICATION)
- **Strict Stop Rule**: You MUST generate the content for "## 🔍 1. COMPLIANCE AUDIT EVALUATION SUMMARY" and "## 🛠️ 2. GRANULAR ISSUE ANALYSIS & AUTOMATED MARKDOWN DIFF" exactly ONCE. Do NOT re-write, duplicate, or output multiple versions of the report under any circumstances.
- Once the Markdown Diff block or the PASSED string is written, you MUST immediately close Section 2 and transition straight into the JSON payload inside Zone 2.

# EXECUTION INSTRUCTION  
Ingest the raw marketing planner source reference payload and execute a multi-layer deep self-auditing pipeline against the original Corporate Compliance Guidelines and the Asset infrastructure truth.

You MUST completely fulfill the mandatory dual-zone layout specified in the system prompt rules:
1. Construct **ZONE 1: THE C-SUITE GOVERNANCE REPORT** by formatting the audit evaluation summary, document control governance matrix, and the explicit Markdown Diff blocks showing the strict changes required. Fully execute the translation process on human-readable elements to match the requested target language variable context.
2. Construct **ZONE 2: THE RESPONDER KNOWLEDGE PAYLOAD** immediately after Zone 1, keeping it strictly as a raw, valid, un-translated JSON object matching the Technical English schema configuration.

**RIGID MOUNTING DIRECTIVE**: You MUST precisely inject the hidden HTML comment delimiters (`<!--START_GOVERNANCE_REPORT-->`, `<!--END_GOVERNANCE_REPORT-->`, `<!--START_RESPONDER_PAYLOAD-->`, and `<!--END_RESPONDER_PAYLOAD-->`) exactly on their own individual lines enclosing their respective data zones. Do not merge or output any conversational text outside these boundaries.

Output the complete multi-zone professional document now.
